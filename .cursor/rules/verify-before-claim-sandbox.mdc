---
description: 改动必须实测验证后再宣称完成；默认沙盒，仅沙盒限制时才申请放宽
alwaysApply: true
---

# 实测验证 + 默认沙盒

## 验证后再调用 / 宣称完成

- 任何代码改动、修复、配置变更：在告诉用户「已修好 / 可用 / 完成」之前，必须先做与改动匹配的**实测**（单元测试、编译检查、对本机进程/端口/接口的核对等）。
- 禁止只改代码、不跑验证就下结论。
- 禁止用推测代替证据：结论必须能指向测试输出、日志行、状态文件或接口响应。

## 默认沙盒

- Shell / 网络相关操作**默认在沙盒内**执行，不要一开始就申请 `all` / `full_network`。
- **仅当**沙盒明确挡住必要操作（权限错误、无法读写所需路径、无法访问必需网络）时，才申请放宽，并说明卡在哪条限制。
- 不要因为「图省事」或「上次用过 all」就默认禁用沙盒。

## 反面例子

- ❌ 改完 `start_server.py` 就说「修复完成」，却没跑 `py_compile` / 相关测试。
- ❌ 一上来就 `required_permissions: ["all"]` 起服务、杀进程、扫全机。
- ❌ 看到仪表盘旧状态就断定「守护进程在循环」，却没核对进程列表与日志时间戳。

## 正面例子

- ✅ 改完后先 `pytest` / `py_compile`，通过后再汇报。
- ✅ 先沙盒执行；若报 sandbox 不支持或权限不足，再申请对应权限并重试。
- ✅ 用进程、端口、状态文件、接口响应交叉验证，再解释现象。
